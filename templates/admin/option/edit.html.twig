{% extends 'base.html.twig' %}

{% block title %}
    {{ parent() }} - Editer un option
{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h1>Editer un option</h1>

        {{ include('admin/option/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

        <a class="btn btn-primary" title="Retourner à la liste" href="{{ path('admin.option.index') }}">
            <i class="fa fa-backward"></i>
        </a>

        {{ include('admin/option/_delete_form.html.twig') }}
    </div>
{% endblock %}
